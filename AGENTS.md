# Repository instructions

- 每次推送代码时，自动将当前工作分支合并到 `main` 并推送 `main`；除非用户明确要求不合并或指定其他目标分支。
- 发布到 GitHub 时忽略 `gh`，使用 Git 直接完成提交和推送。

## 多语言页面规则

- 任何页面、导航、入口、SEO、样式或功能改动，都必须检查并同步所有语言页面，而不能只修改中文和英文页面。
- 目前语言页面包括 English、Deutsch、Español、Français、日本語、Português（巴西）和简体中文；新增页面或入口也必须在各语言版本中可发现、可访问。
- 各语言文案必须根据当地语言习惯重新撰写，考虑自然表达、术语、按钮长度和搜索习惯；禁止直接逐字翻译或把英文文案原样复制到其他语言页面。
- 同步多语言页面时，必须同时检查 `hreflang`、canonical、标题、description、结构化数据、sitemap 和旧 URL，避免破坏现有 SEO。
